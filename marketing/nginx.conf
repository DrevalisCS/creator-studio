server {
    listen 80;
    server_name _;

    root /usr/share/nginx/html;
    index index.html;

    # Make absolutely sure JS/CSS get the right MIME even if the base
    # image ever drops one from /etc/nginx/mime.types.
    types {
        application/javascript  js mjs;
        text/css                css;
        application/json        json;
        image/svg+xml           svg;
        text/html               html htm;
        image/png               png;
        image/jpeg              jpg jpeg;
        image/x-icon            ico;
        image/webp              webp;
        font/woff2              woff2;
    }
    default_type application/octet-stream;

    # Long-lived, immutable static assets
    location /assets/ {
        expires 1y;
        add_header Cache-Control "public, immutable";
        add_header X-Content-Type-Options "nosniff" always;
        try_files $uri =404;
    }

    # Installer scripts served as plain text so `curl | bash` / `iwr | iex`
    # work AND browsers display them inline for "read before running".
    location = /install.sh {
        default_type text/plain;
        add_header Cache-Control "no-cache";
        try_files /install.sh =404;
    }
    location = /install.ps1 {
        default_type text/plain;
        add_header Cache-Control "no-cache";
        try_files /install.ps1 =404;
    }

    # Clean URLs — /pricing resolves to /pricing.html
    location / {
        try_files $uri $uri.html $uri/ =404;
    }

    # ── Security headers ─────────────────────────────────────────────
    add_header X-Frame-Options "DENY" always;
    add_header X-Content-Type-Options "nosniff" always;
    add_header Referrer-Policy "strict-origin-when-cross-origin" always;
    add_header Strict-Transport-Security "max-age=63072000; includeSubDomains; preload" always;
    add_header Permissions-Policy "camera=(), microphone=(), geolocation=(), interest-cohort=(), browsing-topics=()" always;
    add_header X-Robots-Tag "index, follow" always;
    add_header Cross-Origin-Opener-Policy "same-origin" always;
    add_header Cross-Origin-Resource-Policy "same-site" always;
    #
    # Content-Security-Policy — pragmatic profile for a mostly-static
    # marketing site:
    #   * script-src allows the Tailwind CDN + Google Tag Manager
    #     because those scripts are load-bearing on every page.
    #     ``'unsafe-inline'`` is still needed for the gtag bootstrap
    #     block and the PayPal toggle; move those to /assets/ in a
    #     later pass to drop it.
    #   * style-src allows inline styles because boot-intro + consent
    #     banner inject styles at runtime.
    #   * img-src allows data: for favicons + OG placeholders and the
    #     product screenshot CDN origin.
    #   * frame-ancestors 'none' blocks clickjacking / iframe-embed.
    add_header Content-Security-Policy "default-src 'self'; script-src 'self' 'unsafe-inline' https://cdn.tailwindcss.com https://www.googletagmanager.com https://www.google-analytics.com; style-src 'self' 'unsafe-inline' https://fonts.googleapis.com; font-src 'self' https://fonts.gstatic.com; img-src 'self' data: blob: https://www.google-analytics.com https://drevalis.com; connect-src 'self' https://www.google-analytics.com https://*.analytics.google.com https://*.googletagmanager.com https://license.drevalis.com; frame-src 'none'; frame-ancestors 'none'; base-uri 'self'; form-action 'self' https://license.drevalis.com" always;

    # Gzip
    gzip on;
    gzip_types
        text/css
        text/plain
        text/xml
        application/javascript
        application/json
        application/xml
        application/xml+rss
        image/svg+xml
        font/woff2;
    gzip_min_length 1024;
    gzip_vary on;
}
